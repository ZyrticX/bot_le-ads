# 🚀 העלאת הבוט לשרת - מדריך מהיר

---

## 📋 מה צריך לפני שמתחילים?

### בדוק מה יש לך:

**1. שרת (אחד מהבאים):**
- [ ] VPS (DigitalOcean, AWS, Linode, וכו')
- [ ] Cloud Platform (Railway, Heroku, Render)
- [ ] שרת פרטי בבית
- [ ] אחר: ___________

**2. גישה לשרת:**
- [ ] SSH Access (IP + username + password/key)
- [ ] Linux (Ubuntu מומלץ)
- [ ] Windows Server
- [ ] אחר: ___________

**3. פרטי חיבור:**
```
IP Address: ___________
Username: ___________
Password/Key: ___________
```

---

## 🎯 בחר את שיטת ההעלאה המתאימה לך:

### אופציה 1: שרת Linux (VPS) - Ubuntu 🐧
**מומלץ אם:** יש לך VPS עם Ubuntu
**זמן:** 15-20 דקות
**קושי:** בינוני

### אופציה 2: Railway.app ☁️
**מומלץ אם:** רוצה פתרון מהיר וקל (חינמי!)
**זמן:** 10 דקות
**קושי:** קל

### אופציה 3: Docker 🐳
**מומלץ אם:** יודע לעבוד עם Docker
**זמן:** 10 דקות
**קושי:** בינוני-מתקדם

---

## 🐧 אופציה 1: העלאה ל-Ubuntu Server (VPS)

### שלב 1: התחבר לשרת

```powershell
# מה-PowerShell שלך:
ssh username@your-server-ip

# לדוגמה:
# ssh root@123.45.67.89
```

אם ביקשו סיסמה - הזן אותה

---

### שלב 2: הכן את השרת

```bash
# עדכון המערכת
sudo apt update && sudo apt upgrade -y

# התקנת Python וכלים נדרשים
sudo apt install -y python3 python3-pip python3-venv git

# בדוק שPython מותקן
python3 --version
```

צריך לראות: Python 3.9 ומעלה

---

### שלב 3: העלה את הקבצים

**אופציה A: העתקה ישירה מהמחשב שלך**

```powershell
# מה-PowerShell שלך (לא מהשרת!)
# במיקום התיקייה של הפרויקט:

scp -r * username@your-server-ip:~/telegram-leads-bot/

# לדוגמה:
# scp -r * root@123.45.67.89:~/telegram-leads-bot/
```

**אופציה B: דרך GitHub** (מומלץ)

```bash
# בשרת:
cd ~
git clone https://github.com/YOUR_USERNAME/telegram-leads-bot.git
cd telegram-leads-bot
```

**אופציה C: העתקה ידנית**
- דחוס את התיקייה לZIP
- העלה דרך FTP/SFTP (WinSCP, FileZilla)
- פרוס בשרת

---

### שלב 4: הגדר את הבוט בשרת

```bash
# היכנס לתיקייה
cd ~/telegram-leads-bot

# צור סביבה וירטואלית
python3 -m venv venv

# הפעל את הסביבה
source venv/bin/activate

# התקן תלויות
pip install -r requirements.txt
```

---

### שלב 5: צור קובץ .env בשרת

```bash
# צור קובץ .env
nano .env
```

הכנס את המידע הבא:
```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
AIRTABLE_API_KEY=your_airtable_api_key_here
AIRTABLE_BASE_ID=your_airtable_base_id_here
```

שמור:
- לחץ `Ctrl+O` ואז `Enter`
- לחץ `Ctrl+X` לצאת

---

### שלב 6: בדיקה ראשונית

```bash
# הרץ את הבוט לבדיקה
python3 bot.py
```

אם רואה:
```
🚀 Bot is starting...
```
אז זה עובד! ✅

לעצור: `Ctrl+C`

---

### שלב 7: הגדר את הבוט לרוץ תמיד (Systemd)

```bash
# ערוך את קובץ השירות
nano telegram-bot.service
```

**החלף את השורות הבאות:**
```ini
User=YOUR_USERNAME_HERE
WorkingDirectory=/home/YOUR_USERNAME_HERE/telegram-leads-bot
Environment="PATH=/home/YOUR_USERNAME_HERE/telegram-leads-bot/venv/bin"
EnvironmentFile=/home/YOUR_USERNAME_HERE/telegram-leads-bot/.env
ExecStart=/home/YOUR_USERNAME_HERE/telegram-leads-bot/venv/bin/python bot.py
```

**ב:**
- `YOUR_USERNAME_HERE` → שם המשתמש שלך בשרת (למשל: `root` או `ubuntu`)

שמור וסגור (`Ctrl+O`, `Enter`, `Ctrl+X`)

```bash
# העתק את קובץ השירות למיקום הנכון
sudo cp telegram-bot.service /etc/systemd/system/

# טען מחדש את systemd
sudo systemctl daemon-reload

# הפעל את הבוט
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot

# בדוק סטטוס
sudo systemctl status telegram-bot
```

אם רואה `Active: active (running)` - הכל עובד! ✅

---

### שלב 8: ניהול הבוט

```bash
# הפסקת הבוט
sudo systemctl stop telegram-bot

# הפעלה מחדש
sudo systemctl restart telegram-bot

# סטטוס
sudo systemctl status telegram-bot

# צפייה בלוגים
sudo journalctl -u telegram-bot -f

# לוגים אחרונים (100 שורות)
sudo journalctl -u telegram-bot -n 100
```

---

## ☁️ אופציה 2: Railway.app (מהיר וקל!)

### יתרונות:
- ✅ חינמי עד 500 שעות/חודש
- ✅ לא צריך לנהל שרת
- ✅ העלאה קלה דרך GitHub
- ✅ לוגים בזמן אמת

### שלבים:

**1. הכן את הקוד ל-Git**
```powershell
# בתיקיית הפרויקט:
git init
git add .
git commit -m "Initial commit"
```

**2. העלה ל-GitHub**
- צור repository חדש ב-GitHub
- העלה את הקוד:
```powershell
git remote add origin https://github.com/YOUR_USERNAME/telegram-leads-bot.git
git branch -M main
git push -u origin main
```

**3. פריסה ב-Railway**
1. גש ל-[railway.app](https://railway.app)
2. לחץ "Start a New Project"
3. בחר "Deploy from GitHub repo"
4. חבר את GitHub
5. בחר את ה-repository שלך
6. Railway מזהה Python אוטומטית

**4. הוסף משתני סביבה**
1. לחץ על הפרויקט
2. לחץ "Variables"
3. הוסף:
   - `TELEGRAM_BOT_TOKEN` = [הToken שלך]
   - `AIRTABLE_API_KEY` = [ה-API Key]
   - `AIRTABLE_BASE_ID` = [ה-Base ID]

**5. Deploy!**
- Railway עושה deploy אוטומטית
- צפה בלוגים ב-"Deployments"
- הבוט יהיה פעיל תוך דקה-שתיים

**זהו! הבוט רץ 24/7 בחינם! 🎉**

---

## 🐳 אופציה 3: Docker

### יצירת Dockerfile

צור קובץ `Dockerfile`:
```dockerfile
FROM python:3.11-slim

WORKDIR /app

# Copy requirements first for better caching
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of the application
COPY . .

# Run the bot
CMD ["python", "bot.py"]
```

### יצירת docker-compose.yml

```yaml
version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: leads-bot
    restart: always
    env_file:
      - .env
    volumes:
      - ./:/app
```

### הרצה

```bash
# בנייה
docker-compose build

# הפעלה
docker-compose up -d

# לוגים
docker-compose logs -f

# עצירה
docker-compose down

# הפעלה מחדש
docker-compose restart
```

---

## 🔍 בדיקת תקינות אחרי העלאה

### צ'קליסט:

- [ ] הבוט רץ (ראה בלוגים: "Bot is starting...")
- [ ] הבוט עונה ל-`/start` בטלגרם
- [ ] ניתן ליצור עסקה חדשה
- [ ] העסקה מופיעה ב-Airtable
- [ ] הבוט ממשיך לרוץ אחרי reboot (VPS)
- [ ] הלוגים נראים תקינים

### בדיקה מהירה:

```bash
# בדוק שהבוט רץ
ps aux | grep bot.py

# או אם השתמשת ב-systemd:
sudo systemctl status telegram-bot

# ראה לוגים
tail -f /var/log/syslog | grep python
# או
sudo journalctl -u telegram-bot -f
```

---

## 🛠️ פתרון בעיות נפוצות

### הבוט לא מגיב

**1. בדוק שרץ:**
```bash
sudo systemctl status telegram-bot
# או
ps aux | grep bot.py
```

**2. ראה שגיאות בלוגים:**
```bash
sudo journalctl -u telegram-bot -n 50
```

**3. בדוק Token:**
```bash
# ודא ש-.env קיים
cat .env

# בדוק חיבור לטלגרם
curl https://api.telegram.org/bot<YOUR_TOKEN>/getMe
```

### "ModuleNotFoundError"

```bash
# ודא שהסביבה הוירטואלית פעילה
source venv/bin/activate

# התקן מחדש
pip install -r requirements.txt
```

### "Permission denied"

```bash
# תן הרשאות
chmod +x bot.py

# או הרץ כ-sudo
sudo systemctl restart telegram-bot
```

### הבוט מתנתק אחרי זמן

```bash
# ודא שיש Restart=always בקובץ השירות
sudo nano /etc/systemd/system/telegram-bot.service

# צריך להיות:
Restart=always
RestartSec=10
```

---

## 📊 ניטור ותחזוקה

### צפייה בלוגים בזמן אמת

```bash
# Systemd
sudo journalctl -u telegram-bot -f

# Docker
docker-compose logs -f

# קובץ לוג ישיר
tail -f bot.log  # אם הגדרת logging לקובץ
```

### גיבוי

```bash
# גבה את הקוד
tar -czf telegram-bot-backup.tar.gz ~/telegram-leads-bot/

# גבה את Airtable דרך הממשק שלהם
```

### עדכון הבוט

```bash
# עצור את הבוט
sudo systemctl stop telegram-bot

# משוך עדכונים (אם ב-Git)
git pull

# או העתק קבצים חדשים
scp bot.py username@server:~/telegram-leads-bot/

# הפעל מחדש
sudo systemctl start telegram-bot
```

---

## 🎯 סיכום - בחר את השיטה שלך:

| שיטה | קל? | חינמי? | שליטה | מומלץ למי? |
|------|-----|--------|-------|-----------|
| **Railway.app** | ⭐⭐⭐⭐⭐ | ✅ | 🔧🔧 | מתחילים |
| **Ubuntu VPS** | ⭐⭐⭐ | 💰 $5/חודש | 🔧🔧🔧🔧🔧 | מתקדמים |
| **Docker** | ⭐⭐⭐⭐ | תלוי בשרת | 🔧🔧🔧🔧 | יודעים Docker |

---

## 📞 זקוק לעזרה?

**בעיות בהעלאה?**
1. ראה לוגים (הפקודות למעלה)
2. בדוק ש-.env מוגדר נכון
3. ודא שהשרת יכול להתחבר לאינטרנט
4. בדוק firewall (אם יש)

**רוצה עזרה ספציפית?**
ספר לי:
- איזה שרת יש לך? (VPS/Cloud/אחר)
- מערכת הפעלה? (Ubuntu/Windows/אחר)
- מה השגיאה שאתה מקבל?

---

**בהצלחה! 🚀**

Created By Corporation2024 ✨

